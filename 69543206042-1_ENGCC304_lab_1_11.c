#include <stdio.h>

int main(){
    char os[20] , editor[20];

    scanf("%s %s", os, editor);

    printf("OS: %s\n", os);
    printf("Editor: %s\n", editor);
    printf("Ready to code!\n");

    return 0;
}